syntax = "proto3";
package Jubo.JuLiao.IM.Wx.Proto; //命名空间约定

message DeleteFriendTaskMessage {
    string WeChatId = 1; // 商家所属微信号
    string FriendId = 2; // 要添加的手机号码，批量
    int64 TaskId = 3; //任务Id
}